# Platform — Layer 4 (ops console)

| | |
|---|---|
| Backend | SP-14 … SP-18 · L4 spec §9 extras |

Now the overview **may** show KPI tiles — from **snapshots only** (ADR-07).

---

## Dashboard + reports — `FE-AD-KPI` (SP-16)

`GET /platform/dashboard` EP-AD-090. Cards/charts/alerts EP-AD-094…096. Reports EP-AD-091. Export job EP-AD-092/093. Limit 5 exports/hour. Download URLs expire 24h.

**Do not** read live order tables for tiles.

---

## Broadcasts — `FE-AD-PUSH` (SP-14)

EP-AD-080 (and catalog extras 081–085): dual + crit, starts `pending_approval`. Preview before send. Log is append-only.

---

## Billing + legal + flags — `FE-AD-BILL` (SP-17)

Plans, subscriptions, platform invoices, dunning (EP-AD-100…). Replaces light `channel_plans` from SP-04. Legal/help/intro copy. `GET /public/app-config` / feature flags — this UI toggles flags that `/app/session` reads. Maintenance mode.

---

## Support + team — `FE-AD-SUP`

360 search, tickets, impersonation (when catalog row is live — crit + audit). Team invite/disable (EP-AD-154…). Device reset. Backups / scheduled jobs as **status pages** + reason, not raw SSH.

---

## Out of scope

Building Flutter screens, channel catalog forms, warehouse PIN.

## Done

1. Dashboard numbers match snapshot rows, not a live `COUNT(*)` of sub_orders.  
2. Broadcast without second approval does not send.  
3. Feature flag change is visible on the next app session.  
4. Still zero `/channel/products` from this origin.
