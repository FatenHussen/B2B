# Warehouse — Layer 4

**No new floor screens.** Invoices, wallets, and KPI boards are channel/platform.

Keep L3 queues working when `@b2b/api-client` grows finance resources — this app must **not** import them.

Regression: PIN + pick + handover still succeed.
