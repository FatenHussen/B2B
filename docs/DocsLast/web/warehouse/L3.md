# Warehouse — Layer 3 (floor operations)

| | |
|---|---|
| Backend | SP-11, SP-12 sort · `docs/api/catalog/06-warehouse.php` |
| Keep | [L1.md](./L1.md) PIN |

Replace empty home with **queues**. `can()` gates each queue.

---

## Queues + pick + pack — `FE-WH-FLOW`

| EP | Path | UI |
|---|---|---|
| EP-WH-010 | `GET /warehouse/queues` | counts from **real** statuses |
| EP-WH-011 | picking list | lines ordered by aisle/shelf |
| EP-WH-012 | scan | 422 `barcode_not_in_order` |
| EP-WH-013 | manual qty | `manual=true` |
| EP-WH-014 | shortage | `out_of_stock\|damaged\|not_in_location` — does not silently fill |
| EP-WH-015 | complete pick | → `to_pack` |
| EP-WH-016/017 | pack verify / complete | barcode match; weight → grams integer; QR labels |

---

## Handover — REQ-IN-02

`GET /warehouse/handovers/pending` · `POST /warehouse/handovers` (EP-WH-018/019) → `awaiting_rep_confirm` + 4-digit `temp_code`. **Does not** deduct stock and **does not** set `on_the_way`.

Rep confirms in the Flutter app. Return trip EP-WH-020 restocks undelivered. `wallet_matched` may be null until L4 — do not fake cash match.

---

## Receiving + stocktake

EP-WH-021/022 goods in + QC (`photos` media ids).  
EP-WH-023 start/lines/submit — the counter **must not** see `on_hand` / `book_qty`.  
EP-WH-024 approve: dual + SOD (approver ≠ counter).

---

## Return sort — EP-WH-030

Per line `resalable|damaged|expired`. This is warehouse condition, not the shop’s request.

---

## Done

1. Scan of a foreign barcode → 422.  
2. Open handover alone: stock still `reserved`, order not `on_the_way`.  
3. Stocktake payload has no book qty.  
4. Same user cannot approve their own count.
