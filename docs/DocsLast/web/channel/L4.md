# Channel — Layer 4 (finance, notify, content, dashboard)

| | |
|---|---|
| Backend | SP-13 … SP-16 |
| Keep | [L3.md](./L3.md) |

---

## Finance — `FE-CH-FIN` (SP-13)

| EP | Path | Gate |
|---|---|---|
| EP-SC-080 | invoices list | `sc.finance.view` |
| EP-SC-081 | credit note | dual |
| EP-SC-082 | void invoice | dual |
| EP-SC-083 | office payment | `sc.finance.payment` + SOD-01 (not the confirmer) |
| EP-SC-084 | settle rep | `sc.reps.settle` + `operation_no` |
| EP-SC-085 | aging | buckets 0–30 / 31–60 / 61–90 / >90 |
| EP-SC-086 | retailer credit | `on_exceed` warn\|block\|manual |

Integers. Office payment does **not** increase the rep wallet.

---

## Notify — `FE-CH-PUSH` (SP-14)

Send EP-SC-090 (`queued` immediately if `sc.notify.send`). Templates EP-SC-091. Log EP-SC-092. Hiding an in-app inbox item on mobile must **not** delete this log.

---

## Content + loyalty — `FE-CH-CMS` (SP-15)

Intros, banners, sliders, loyalty rules/rewards. Targeting by zone/activity. Stats from events — no mock CTR.

---

## Dashboard — `FE-CH-KPI` (SP-16)

`GET /channel/dashboard` reads **daily snapshots** (ADR-07). Do not query live `sub_orders` for tiles. Margins EP-SC-123: crit; `null` if no cost — never invent a margin. Reports + export → `job_id`.

---

## Done

1. User with confirm+payment cannot post office payment (`sod_violation`).  
2. KPI tiles still work if OLTP is slow (snapshots).  
3. Banner targeted to zone 12 is absent for zone 13 on Flutter.
