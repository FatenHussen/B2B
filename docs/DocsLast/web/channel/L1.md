# Channel — Layer 1 (sign-in shell)

| | |
|---|---|
| Backend | SP-01 `/channel/auth/*` only |
| Kit | [`../../_shared/react-client.md`](../../_shared/react-client.md) |
| Dev | `next dev -p 3001` |

OTP, optional picker, empty office. **No** catalog fetches.

---

## Scaffold

```bash
npx create-next-app@15 apps/channel-web --typescript --app --tailwind --eslint --src-dir --use-npm
```

```
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_X_CLIENT=channel-web
```

```
src/app/(auth)/login/phone  login/otp
src/app/(app)/select-channel  home
src/features/auth  channel  shell
```

Bearer in `sessionStorage`. Not Sanctum cookies.

---

## Auth

`POST /channel/auth/request-otp` `{ phone }` — no `purpose`.  
`POST /channel/auth/verify-otp` `{ otp_id, code }`.

| `channels.length` | Next |
|---|---|
| 1 | Set `X-Channel-Id`, `/home` |
| > 1 | `/select-channel` |
| 0 | Server message; do not invent a channel |

No catalog logout endpoint in L1: `clearSession()` → `/login/phone`.

Sidebar from `can()`. L2+ items disabled with «يتاح بعد تشغيل العمليات» — **no page, no fetch**. `/home`: channel name only. No charts.

---

## Done

1. Seeded phone + log OTP lands on `/home`.  
2. DevTools: **zero** `/channel/products`.  
3. Sign-out + refresh does not restore the shell.
